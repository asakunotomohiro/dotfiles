# 本ファイルにGitディレクトリ上で表示されるプロンプトを設定する。
#	ブランチを表示させられる。
#	しかし、その専用ファイルを取得しなければならず、ちと面倒なのでやめた。

. ~/git-prompt.sh
export GIT_PS1_SHOWDIRTYSTATE=1
export PS1='\w$(__git_ps1 " (%s)")\$ '

# 以上。
# vim: set ts=4 sts=4 sw=4 tw=0 ff=unix fenc=utf-8 noexpandtab:
