<!-- vim:set ts=4 sts=4 sw=4 tw=0: -->

# 概要。
各ソフトウェアに対する設定ファイルを保存する場所になる。  
そのため、設定ファイルが混在するため、見にくくなることだろう。  
それを本ファイルで説明することにより、少しで個緩和できたらと思う。  
※新規登録日は、、ずれてしまった。  

## 事前準備物
以下の資材が導入済みであること。  

### Git
バージョン管理システムのこと。  
vimエディタで必要なプラグインを取得するのに必要。  

[https://gitforwindows.org/](https://gitforwindows.org/)  
[https://git-scm.com/book/ja/v2](https://git-scm.com/book/ja/v2)  

#### \_gitconfig
2021/02/11：新規登録。  

### vim
Windows・Macなどなどで使うためのエディタ。  

[https://www.kaoriya.net/software/vim/](https://www.kaoriya.net/software/vim/)  
[https://github.com/koron](https://github.com/koron)  
[https://www.vim.org/download.php#pc](https://www.vim.org/download.php#pc)  
[https://github.com/koron/vim-kaoriya](https://github.com/koron/vim-kaoriya)  

必須  
できるならば、Python導入済みのvimエディタであるこが望ましい。  


### firefox
vimエディタからマークダウンファイルをプレビューで見るために必要。  

[https://www.mozilla.org/ja/firefox/new/](https://www.mozilla.org/ja/firefox/new/)  

### Ctags
あればよし。無くても問題ない。  

[https://github.com/universal-ctags/ctags](https://github.com/universal-ctags/ctags)  

#### Golang
あればよし。無くても問題ない。  

[https://golang.org/](https://golang.org/)  

### 秀丸エディタ
あればよし。無くても問題ない。  

[https://hide.maruo.co.jp/software/hidemaru.html](https://hide.maruo.co.jp/software/hidemaru.html)  

### サクラエディタ
これぐらいは用意できるだろう？  

[https://sakura-editor.github.io/](https://sakura-editor.github.io/)  


## makeLinkfile
下記のAutoHotKey・[g]vimrcファイルのシンボリックファイルを各所に配置するPowerShellになる。  

* AutoHotKey：ドキュメントディレクトリに配置する。  
* [g]vimrc：HOMEディレクトリに配置する。  
* gitignore\_global.txt：ドキュメントディレクトリに配置する(Sourcetree専用)。  
* hgignore\_global.txt：ドキュメントディレクトリに配置する(Sourcetree専用)。  

2021/02/13：新規登録。  

## hidemaru.hmereg
秀丸エディタの設定ファイル  

※ **エディタからの読み込み方法があるため、それを用いること。**  
他の方法で読み込む場合、設定が壊れる可能性があるため気をつけること。  

※上記のmakeLinkfileを実行したときに、vimbackupディレクトリをホームディレクトリ配下に作成する。
そこに、秀丸エディタで編集したファイルのバックアップファイルを作るようにしている。  

2021/01/05：新規登録  
2021/02/11：新規登録。  

## sakura.ini
サクラエディタの設定ファイル。  

## TeraPad.ini
テラパッドエディタの設定ファイル。  

## AutoHotkey
Windows専用の設定ファイルになる。  
正確に言うならば、スクリプトファイルになるだろう。  
その記述方法により、キーボードショートカットになりえる。  
しかし、任意のソフトウェアを起動する場合、フルPathをべた書きする必要があるため、今のところ使い回すに至らない。  
使う端末ごとにPathを書き換える必要が出てくる。  
2021/01/10：新規登録  
2021/02/12：新規登録。  
exe：v1.1.33.2 Unicode 64-bi  

### 以下、ホットキー(ソフトウェア起動用)
ソフト起動時の通常Path  

- C:\Program Files\～～～
- C:\Program Files (x86)\～～～

※もちろん例外もある。

* Winキー＋[標準キー]  
  * f	：Firefoxブラウザ(※)  
  * h	：秀丸エディタ(※)  
  * p	：ペイント(※)  
  * v	：Gvimエディタ(※)  
  * c	：googleChromeブラウザ  
  * g	：Git(Sourcetree)  

* Winキー + Ctrlキー + [標準キー]  
  * s	：サクラエディタ(※)  

* Winキー + Altキー + [標準キー]  
  * r  
  * e	：Evernote  
  * i	：IDE(Eclipse)  
  * j	：Jasper soft  
  * k	：Kindle(Amazon)  
  * m	：minecraft-game  
  * o	：Origin-game  
  * s	：Steam-game  
  * x	：Dropbox  

* Winキー + Altキー + Shift + [標準キー]  
  * i	：IDE(MS-VSCode)  

※WindowOS標準のショートカットキーを上書きすることになるため、気をつけること。  

### 以下、ホットキー(ソフトウェア内での挙動)
こちらも通常のショートカットキーを潰している可能性がある。  

* Ctrl+h	：バックスペースキー  
* Ctrl+w	：ウィンドウ終了(タブ・子ウィンドウ)  
* Win+q	：ウィンドウ終了(親ウィンドウまるごと)  

#### Gvim
個別対応  

* Win+t	：新規タブ作成  
* Win+w	：タブClose  
* Win+m	：現在のバッファを新規タブに移動  
* Alt+Space	：アクティブウィンドウの最小化。  
* Win+Alt+d	：現在のバッファを終了  

#### 秀丸エディタ
個別対応  

* Ctrl+g	：Grep検索ウィンドウ表示  
* Ctrl+j	：指定行移動  
* Win+Alt+f	：全画面表示(割り当てキーをいずれ変更する？)。  

#### Eclipse
個別対応  

* Ctrl+g	：Grep検索ウィンドウ表示  
* Ctrl+j	：指定行移動  



## vimrc・gvimrc
Vimエディタの設定ファイル。  
HOMEディレクトリ直下に配置するのが基本になる。  
また、本来のファイル名は、.vimrc・.gvimrcになる。  
Windowsの場合は、\_vimrcなどを使っていたが、今は他のOSに併せてアンダーバーからドットを使えるようになっている。  
そのため、Windowsで使う場合もファイル名をドットに書き換えた方が良い。  
※MacやLinuxの場合は隠しファイルになるため、ドットをアンダーバーに置き換えて登録している。  
2021/01/15：新規登録  
2021/02/12：vimrc新規登録。  
2021/02/13：gvimrc新規登録。  

### 内容
バックアップファイルは、OS問わず、ホームディレクトリ直下の.vim\_backupディレクトリを使うようにしている。  
ただし、スワップファイルの作成は、カレントディレクトリに作るようにしている。  
その理由は言わずもがな。ファイルサーバでの編集をする場合は、当然のようにカレントディレクトリが求められるからだ。  


#### プラグイン無関係設定
日本語関係や多バイト言語の設定をしている。  
しかし、これは元からあった設定を残しているだけで、具体的な処理内容までは把握していない。  
フォント設定もどこかのホームページからのコピペだったはず。  

C言語限定のデバッグ画面表示遷移処理は、どこかのホームページからコピペしただけで、どのように使うのか不明。  
標準のプラグインのため、外部から取り込まずに利用できるのはいい(デバッグの使い方がわからず、恩恵にあずかれていないけど)。  

使いこなせないのだが、vimgrep用にウィンドウが分割する設定をしている。  
タグなどの対となるキーワード移動も標準のプラグインを利用している。  

Windows版のvimは、香り屋さんのを使っている。  
そのための設定も(元から)入っている。  

バックアップファイルの作成箇所は1カ所に固めたいため、OS関係なくまとめる設定をしている。  
他でも呟いているが、スワップファイルだけはカレントディレクトリに放置したままだ。  

文字コードの設定もどこかのホームページから真似したのだが、UTF-7やJISファイルは開けない。  

折りたたみ設定を有効化しているが、ファイルを開いた時点では、折りたたみを開いた状態にしている。  

* za	：トグル  
* zo	：折りたたみが開く  
* zc	：折りたたむ  
* zR	：すべての折りたたみを開く  
* zM	：すべての折りたたみを閉じる。  

秀丸エディタやサクラエディタのように、タブバーも使いやすいように設定している。  

* \<Leader\>tn	：新規タブを開く。  
* \<Leader\>tc	：タブを閉じる。  
* \<Leader\>tm	：カレントバッファを新規タブに移動する。  
* \<Leader\>to	：カレントタブ以外閉じる。  
* gvimrc
  * \<C-Tab\> gt	：Ctrl+Tabで、タブ移動(左から右)。  
  * \<S-C-Tab\> gT	：Ctrl+Tabで、タブ移動(右から左)。  

vimエディタの設定ファイルを開けるようにもしてある。  

* \<Leader\>ev	：vimrcを開く。  
* \<Leader\>eg	：gvimrcを開く。  
* Windows限定  
  * \<Leader\>eh	：AutoHotKeyを開く。  

Windows限定として、
`<Leader>a`
をすることにより、カレントバッファの全選択ができるようにしている。  

gvimrc限定の設定としては、IMEの状態で色を変化させるとか、ウィンドウサイズを固定したままエディタ起動するとか、日本語や多バイト文字への対処などなど。  

などなどの設定説明は、ざっくりではあるが、コメントを付けているため、混乱することはないはず。  

##### 困りごと(vimrc)。
カーソルの色設定が反映されない。  
アクティブカーソルを線だけにしたいのに、塗りつぶしになってしまい、非常に醜くなっている。  


##### 困りごと(gvimrc)。
`vim-gitgutter`での色設定が反映されない。  
そのため、vimrcとgvimrcで同じ設定を試みたが、撃沈した。  
vimエディタ本体をアンインストール・インストールを何度か行ってみたが、徒労に終わった。  

cui版は、色がどぎつく、とてもじゃないが、実用に耐えられない。
しかし、対処できずに困っている。  

gui版さえまともに動いてくれれば、後は何とかなると思っている・・・そう思い込んで、対処を後回しにしている。  

#### プラグイン用設定
キーマップ。
もしくは、備忘録。

* minpac  
PackUpdate	：プラグインのアップデート。  
PackClean	：不要プラグインの削除。  
PackStatus	：プラグインのステータス確認。  


* fern.vim  
エクスプローラで有名？  
しかし、使い勝手は悪い。  
\<Leader\>er	：エクスプローラを開く(前面表示)。  
F13	：エクスプローラを開く(左ペイン)。  
※切り取りなどは、既存のキーである `M` にマッピングされている。怖くて使うのをためらうプラグインではあるが、他に見つけられず、泣く泣く使っているプラグインでもある。  

* vim-gitgutter  
\<leader\>ha	：アクティブハンクをステージングする。  
\<Leader\>hu	：ハンク部分のステージングを取り消す。  
\<Leader\>hp	：ハンク部分のプレビュー(変更前の状態と比較)  
]c	：押下により、次のハンク(変更点のかたまり)  
[c	：押下により、前のハンクへ移動  
icやacもあるのだが、よくわからない。  
F18	：ハイライト表示切り替え(トグル)  
Macの場合、異なる挙動をするため、利用禁止。  

* vim-fugitive  
\<leader\>ga	：git add  
\<leader\>gc	：git commit  
\<leader\>gb	：git blame  
\<leader\>gl	：git log  
\<leader\>gh	：新規タブでgit history  
\<leader\>gp	：git push  
\<leader\>gf	：git fetch  
\<leader\>gd	：git diff  
\<leader\>gr	：git rebase -i  
\<leader\>gg	：git grep  
\<leader\>gm	：git merge  
\<leader\>gs	：gitステータスを新規タブで開く。  
s：ステージ(add)する。  
u：ステージした物を取り除く(undo)  
=：diffをその場で開く  
dv：diffのvisual(下ペインで開く)  
\<Enter\>：編集する  
X：変更を取り消す  
以下、コミット操作  
	* cc：コミット  
	* ca：直前のコミットを変更する形でコミットする(git commit --amend)  
	* ce：査前のコミットを変更する形でコミットするが、コミットメッセージを変更しない(git commit --amend --no-edit)  
	* cw：査前のコミットのコミットメッセージのみを変更する。  
	* cvc：verboseモードでコミットする(git commit -v)  
	* cf：fixup!でコミットする(git commit --fixup=)これを実行して査後に\<Tab\>\<Enter\>押下後HEADにfixupする。  

* tagbar  
F8キー	：タグ一覧表示切り替え(トグル)  

* undotree  
F17キー	：アンドゥツリー切り替え(トグル)  
すべての変更には、タイムスタンプが表示され、シーケンス番号も付与される。  
\>番号\<としてマークされる。復元可能な場合は、\{番号\}で表現されている。  
保存された変更は`s`としてマークされ、`S`は、最後に保存された変更を示す。  
J・K	：Tree移動(下記とどう違う？)  
\<・\>	：保存状態に戻す(編集ファイルを変更しながら履歴を確認できる)。  
T	：変更履歴の時間表示の切り替え。  
q	：ツリーペイン終了。  
Tabキー	：本来の編集ペインに戻る。  

* gundo.vim  
F17キー	：アンドゥツリー切り替え(トグル)  
そもそもvimエディタがPythonをサポートしている前提であるため、通常は使えない(ゆえに私も使えていない)。  
`:echo has('python3')` の実行で、1が表示されていれば使える。  
上記のアンドゥツリーより、こっちのほうが有名なようだ。  

* open-browser.vim  
firefoxブラウザを開くように設定したが、OSや端末環境により、インストールPathを変更する必要が出てくる。  
また、\<leader\>pで、ブラウザを開く。  
そして、マークダウンファイルのみ限定してブラウザが開くようになっている。  

* vim-session ・ vim-misc  
なぜか対になっての導入が必須になったプラグインだ。  
セッション管理として使うつもりで導入したが、実際は使っていない。  
正確には使いこなせていないのだが・・・。  
`~/.vim\_backup/sessions/`  
拡張子は、既存から変更しており、 `vimSession` を使っている。  
:SaveSession　⇒　~/.vim\_backup/sessions/default.vimSession  
:SaveSession hoge　⇒　~/.vim\_backup/sessions/hoge.vimSession  
:OpenSession　⇒　~/.vim\_backup/sessions/default.vimSession  
:OpenSession hoge　⇒　~/.vim\_backup/sessions/hoge.vimSession  
:ViewSession　⇒　保存済みのセッションが一覧として表示される。  
:DeleteSession　⇒　保存済みのセッションが一覧として表示され、番号を入力することで、それに紐付くセッションファイルが削除される。  
:CloseSession　⇒　現在開いているセッションが終了する。  
:OpenTabSession　⇒　現在のバッファを開きつつ指定のセッションを開く。  
:SaveTabSession　⇒　？  
:AppendTabSession　⇒　？  
:CloseTabSession　⇒　？  


#### プラグイン用設定(未導入時エラー)
プラグインを導入していない場合エラーになるが、当然導入判定処理により、エラーになるのを回避している。  

* vim-gitgutter  
ステータスラインに、Git用の変更などの個数を表示するように設定している(右寄せ)。  


#### プラグイン用設定(変数変更のみ)
未導入でも問題ない設定。

* vim-showmarks  
起動時にマーク表示する設定をしている。  
問題点：この設定が何を表しているのか理解していない。  

* vim-lsp  
lsp\_settings\_filetype\_go
これって、何をしている設定なのだろう(他者の設定をコピペ)。  

* vim-lsp-settings  
lsp\_settings['gopls']
これって、何をしている設定なのだろう(他者の設定をコピペ)。  

* vim-lsp-snippets  
UltiSnips\*="\<tab\>"
これって、何をしている設定なのだろう(他者の設定をコピペ)。  
残念ながら動かない。そのため、よくわかっていないまま放置しているという最悪な設定になっている。  

* vim-gitgutter  
変更などの箇所を色づけしているのだが、それが反映されない。  
しかし、一応は設定しているため、設定が反映されたら重宝することだろう。  

* fern.vim  
ファイルやディレクトリなど大きく変更を伴うような作業は無効化したい。  
そのため、以下のように設定しているのだが、無効化できない。  
`noremap <Nop>newFile <Plug>(fern-action-new-file)`  
困った。  



#### プラグイン本体
OS問わず、プラグイン管理などは、ホームディレクトリ直下の.vimディレクトリを使うようにしている。

k-takataさん作のminpacでプラグインを管理している。  

IDEに近づけるためのプラグインをいくつか導入し、コンパイル環境も用意している。
ただし、コンパイラが導入済みであることを前提にしているため、なかなか他の環境での実現は厳しいだろう。  
直近はGo言語を考えているが、なかなか技術が身につかないため、エディタ機能のマクロ自動記録を使うのがせいぜいだろう。  

環境によっては、プラグイン導入が不可能な場合があると思っている。  
その場合は、導入せずに運用できるよう、プラグイン未導入の場合の判断処理を入れている(if文での制御なので、たいしたことではないが、重要かな)。

##### previm
マークダウンファイルを作成後の確認に使う。  
如何せん、望んだような感じに思えない・・・きっと他のプレビューも似たり寄ったりだろうから有名なプラグインであるこれを選んで使っている。  

##### open-browser.vim
上記のマークダウンファイルをブラウザで開くためのプラグイン。  
上記の思い描いた内容と異なると思っているのは、ブラウザが原因かもしれない。  
しかし、その都度ブラウザを切り替えることができず、指定したブラウザ1種類だけの利用になり、特別な見栄えの悪さもないので、他のブラウザで試すこともなくだらだら使い続けている。  
要は、不満はないと言うことなのだろう。  

##### IDE用プラグイン
以下をそろえることで、IDEのようになる？

* async.vim  
* asyncomplete.vim  
* vim-lsp  
* vim-lsp-settings  
* vim-lsp-snippets  

本物のIDEのような挙動に近づけることは無理だと思い、諦めた。  
なかなか補完機能が望んだとおりに動いてくれない。  

細かい設定をいじったつもりだったが、そこまでマニュアルを熟読したわけではないのが原因だろう。  
1ヶ月近く掛けてvimrcをいじったが、まだまだ未熟なままだ・・・これ以上は、さすがに時間を掛けられない。  

よくわかっていないが、導入することで、IDEに近づける説明をしてあったプラグインを盲目的に導入してるものもある。  

以下は、シンタックスチェックを行うプラグイン。  

* syntastic  

以下は、IDE用というよりは、プログラミング機能強化に近いプラグインになる。  

* tagbar  

プログラミングをする場合、似たり寄ったりの定義した単語を行ったり来たりすることが多い。  
そのため、このプラグインを用いて、簡単に移動するための項目一覧表示をする。  


以下は、IDE用というよりは、補完機能強化に近いプラグインになる。  

* asyncomplete.vim  
* asyncomplete-lsp.vim  

以下は、IDEに標準搭載しているセッション管理に近いプラグインになる。  

* vim-session  
* vim-misc  

##### プログラミング言語用プラグイン
今のところ以下を使っている。  

* vim-goimports  
プログラミング言語で利用するプラグインのひとつ。  
Go言語のインポート作業に必要な作業を自動でプラグインが担ってくれる。  
これがなければvimでプログラムを書こうと思わなかったぐらいだ。  
本当は、もっと他に優秀なのはあるが、多機能であり、手に余るようで、必要最小限のプラグインを選んだ次第だ。  
コンパイル環境が古い場合、動かない可能性もある。  

* vim-quickrun  
特定の言語用と言うより、プログラミングに必要なコンパイル環境と言えるプラグインだろう。  
ただし、前述通り、コンパイル環境があることを前提にしている。
また、コンパイル環境が古い場合、動かない可能性もある。  

##### ctrlp.vim
ファイル検索プラグイン。  

有名なので導入したが、そもそも私は必要なファイルをディレクトリにあるファイルから直接Gvimエディタに投げて開くため、あまり使わないプラグインでもある。  

プログラミング規模が大きくなる、もしくはそれに準じたファイル数になれば重宝することだろう。  
私の場合は、同名ファイルの異なるPathを編集することが多いため、編集する場合は、わざわざPathをホームディレクトリからたどらなければ、違うファイルを変更してしまう心配を持っており、なかなか有効活用できていない。  

今後は、意識して活用しようと思う。  
せっかく導入したのだから・・・。  


##### Git用プラグイン
以下の2種類を組み合わせて使う人が多いようなので、私も真似をした。  

* vim-gitgutter  
* vim-fugitive  

1つ目は、ファイル内の差分を視覚効果で表してくれるプラグインになる。  
2つ目は、Gitディレクトリ上のステータス確認に使うプラグインになる。  

※vimエディタが古い場合、正常に動かないことがあるため、気をつけること。  


##### vim-showmarks
Windows使いであれば、秀丸エディタを活用していることだと思う。  
もしくは、サクラエディタだろうか。  
どちらにしても、現在行をマークやブックマークとして設定することができる。  

vimエディタでもできるのだが、なかなか視認しづらいと言うか、マーク箇所が全くわからないため、視覚できるようにしたプラグインになる。  
ただ、それでもマーク場所に移動しにくいことに変わりは無い。  

私の知らない設定があるとか？  

##### gundo.vim
アンドゥ・リドゥをGitのような形で遡れる。  
Pythonを含んだコンパイル済みのvimエディタが必須になる。  

##### undotree
アンドゥ・リドゥをGitのような形で遡れる。  

##### 他には、、、
IDEの機能として、Tabキーで補完候補を確定させるショートカットキーがある。  
それに倣ったプラグインを使おうと思った時期もある。  
しかし、通常のTabキーが無効化されるため、泣く泣く無効化したプラグインでもある。  

他には、Googleカレンダと連携できるプラグインもある。  
さすがに、携帯電話を使うため、わざわざvimエディタでカレンダ参照は不要かと思い、無効化している。  

#### 外部プログラムの利用。
ctagsプログラムの利用により、tagbarプラグインの利用もあり、プログラム開発用に利用する。  
そこまでプログラム規模が大きくない限りは、ありがたさはわからないが、一応導入している。  
小さいプログラムぐらいは組めるようになりたいし、、、そこから大きなプログラムに発展できればと願っている。  
Go言語用はないので、設定する必要はあるが・・・。  





### vimファイル
拡張子がvimの場合、ファイル形式別の設定ファイル。  

配置場所は、 `~/.vim/after/ftplugin` 配下に置く。  


### プラグイン利用前提
外部プログラムを利用する前提のプラグインが存在する。

* gundo.vim  
Python2.4以上を導入済みであること。  


## 一太郎
バックアップファイルを作成するように設定してあるが、 ~~ゴミ~~ バックアップファイルがカレントディレクトリに生成される。  
気持ち悪いので、無効化しているが、なぜ指定した場所に保存しないのだろうか。  

ちなみに、バックアップファイルの作成場所は、vimのバックアップファイルと同じディレクトリ(そこに、専用のディレクトリを作って、その配下ではあるが)。  
`~/.vim\_backup/Taro/`  

## ATOK
Windows版は設定をバックアップできるが、Mac版はできない。  
なしか。  



## gitignoreファイル
git設定  

2021/02/11：新規登録。  

## メモ\_YYYYMMDD(テキストファイル)
外部でメモをとるためのひな形  

2021/02/14：新規登録。  

## マウス設定(M-DUX70BK).xml
ゲーム用にボタン設定をしている。  













































## ライセンス
GPL-ver2  

改造を含む利用可能なソースコードと同一条件でのライセンス  
そもそも隠す物ではないはずなので、このライセンスで十分と思っているが・・・。  
そんなことより、バレンタインデーに、異性にPushするのではなく、GithubにPushするって・・・あぁ・・・。  

以上。  
